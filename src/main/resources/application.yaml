spring:
  datasource:
    username: ahmed
    password: ahmed
    url: jdbc:mysql://localhost:3306/sport?useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8090

